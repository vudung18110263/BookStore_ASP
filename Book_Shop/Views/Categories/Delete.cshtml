@model Book_Shop.Models.Category

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div class="admin__layout">
    <table class="admin__table">
        <thead>
            <tr class="admin__header">
                <th style="text-align:center">Name</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>@Html.DisplayFor(model => model.name)</td>
            </tr>
        </tbody>
    </table>
    @using (Html.BeginForm("Delete", "Categories", FormMethod.Post, new { style = "margin-left: auto" }))
    {
        @Html.AntiForgeryToken()

        <div class="pagination">
            <a href="/Categories/Index" class="action-link">Back to list</a>
            <a href="/Categories/Index" class="action-link"><input type="submit" value="Delete" /></a>
        </div>
    }
</div>
